name: food101
accelerator: gpu
devices: 1
precision: 16
max_steps: 5000
val_check_interval: 500
data:
  dataset: food101
  root: data/
  size: 224
  batch_size: 128
  workers: 4
model:
  model_name: vit-b16-224-in21k
  optimizer: sgd
  lr: 0.03
  momentum: 0.9
  weight_decay: 0.0
  scheduler: cosine
  warmup_steps: 500
